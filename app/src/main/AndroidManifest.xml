<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.empresadelivery.empresadelivery">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Subirnombresdedescuentos"></activity>
        <activity android:name=".Subirfamilia" />
        <activity android:name=".Subircremaempresa" />
        <activity android:name=".Subiradiconalempresa" />
        <activity android:name=".Administrartiposdepago" />
        <activity android:name=".EditarEmpresa" />
        <activity android:name=".Dashboardempresa" />
        <activity android:name=".Iniciosistema">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Datosdeempresa" />
        <activity android:name=".Nuevodeclientes" />
        <!--
         The API key for Google Maps-based APIs is defined as a string resource.
         (See the file "res/values/google_maps_api.xml").
         Note that the API key is linked to the encryption key used to sign the APK.
         You need a different API key for each encryption key, including the release key that is used to
         sign the APK for publishing.
         You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBqXKFqIrch4W9hfVJXc_G-fVrBSakrXUg" />

        <activity android:name=".Manejodeusuarios" />
        <activity android:name=".Agregaradicionales" />
        <activity android:name=".Listaproductos" />
        <activity android:name=".Subirproductos" />
        <activity android:name=".Mapa" />
        <activity android:name=".Map" />
        <activity android:name=".Mostrarpedidos" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>